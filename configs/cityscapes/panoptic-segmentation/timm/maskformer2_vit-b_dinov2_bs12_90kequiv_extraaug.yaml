_BASE_: maskformer2_vit-b_dinov2_bs1_90kequiv.yaml
SOLVER:
  IMS_PER_BATCH: 12
  # Cityscapes has 2975 images in training set, 500 in val
  MAX_ITER: 960000 # Formula: (# images * # epochs / batch size)
  CHECKPOINT_PERIOD: 12395 # Every 50 epochs
TEST:
  EVAL_PERIOD: 12395 # Every 50 epoch
INPUT:
  ROTATION_AUG: True
  GAUSSIAN_AUG: True
OUTPUT_DIR: vit_b_dinov2_Cityscapes_90kequiv_extraaug