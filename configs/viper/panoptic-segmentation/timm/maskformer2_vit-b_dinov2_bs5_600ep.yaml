_BASE_: maskformer2_vit-b_dinov2_bs1_600ep.yaml
SOLVER:
  IMS_PER_BATCH: 5
  # VIPER has 13367 images in training set, 4959 in val
  MAX_ITER: 1604040 # 600 epochs (# images * # epochs / batch size)
  CHECKPOINT_PERIOD: 26734 # Every 10 epochs
OUTPUT_DIR: vit_b_dinov2_VIPER_600ep
TEST:
  EVAL_PERIOD: 26734 # Every 10 epoch